apiVersion: v1
kind: Service
metadata:
  name: {{ include "assessment-ms.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      protocol: TCP
  selector:
    {{- include "assessment-ms.selectorLabels" . | nindent 4 }}