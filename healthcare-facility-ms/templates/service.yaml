apiVersion: v1
kind: Service
metadata:
  name: {{ include "healthcare-facility-ms.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      protocol: TCP
  selector:
    {{- include "healthcare-facility-ms.selectorLabels" . | nindent 4 }}