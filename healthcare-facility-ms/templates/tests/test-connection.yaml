apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "healthcare-facility-ms.fullname" . }}-test-connection"
  labels:
    {{- include "healthcare-facility-ms.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "healthcare-facility-ms.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
