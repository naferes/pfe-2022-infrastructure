apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "oauth-ms.fullname" . }}-test-connection"
  labels:
    {{- include "oauth-ms.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "oauth-ms.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
