apiVersion: v1
kind: Service
metadata:
  name: {{ include "oauth-ms.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      protocol: TCP
  selector:
    {{- include "oauth-ms.selectorLabels" . | nindent 4 }}
