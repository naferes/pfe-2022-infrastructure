apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "back-office.fullname" . }}-test-connection"
  labels:
    {{- include "back-office.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "back-office.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
